class News {
  constructor(id, title, picture, date, body) {
      (this.id = id),
      (this.title = title),
      (this.picture = picture),
      (this.date = date),
      (this.body = body);
  }
}
